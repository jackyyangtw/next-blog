# Mobile application

This Expo / React Native application is the iOS and Android client.

Run `pnpm dev:mobile` from the repository root, then open it in Expo Go, an
Android emulator, or an iOS simulator. Platform-neutral API contracts belong in
`@jacky-dev/types`; reusable request logic belongs in `@jacky-dev/api`.
