# Web application

The Next.js 16 website lives here. Keep Next.js-only capabilities such as Route
Handlers, Server Actions, NextAuth, and Sanity server clients in this app.

Run `pnpm dev:web` from the repository root to start it on port 3200.
