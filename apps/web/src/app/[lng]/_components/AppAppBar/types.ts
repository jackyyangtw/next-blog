export interface AppBarLabels {
  colorMode: {
    dark: string;
    light: string;
    system: string;
  };
  language: string;
  login: string;
  navigation: {
    posts: string;
    studio: string;
  };
}
